<h3><kbd>CTRL+ENTER</kbd> Submits The Nearest Form (DEFAULT BEHAVIOUR)</h3>

<div class="row">
  <div class="col-sm-4">
    <form class="form" {{action "submit" on="submit"}}>
      <div class="form-group">
        {{textarea-autosize class="form-control" max-height="100px" placeholder="Ctrl+Enter will submit nearest form."}}
        <p class="help-block">
          Press Ctrl+Enter to submit the form that this textarea belongs to. The label will flash success colour
          once the submit event is triggered.
        </p>
      </div>
    </form>
  </div>
  <div class="col-sm-8">
    {{!-- @formatter:off --}}
<pre>\{{textarea-autosize class="form-control" max-height="100px" placeholder="Ctrl+Enter will submit nearest form."}}
</pre>
{{!-- @formatter:on --}}
  </div>
</div>

<h3><kbd>CTRL+ENTER</kbd> Submitting Turned Off</h3>

<div class="row">
  <div class="col-sm-4">
    <form class="form" {{action "submit" on="submit"}}>
      <div class="form-group">
        {{textarea-autosize class="form-control" ctrlEnterSubmitsForm?=false max-height="100px"
                            placeholder="Ctrl+Enter will NOT submit nearest form."}}
        <p class="help-block">
          You can turn off Ctrl+Enter form submission by supplying <code>ctrlEnterSubmitsForm?=false</code>.
        </p>
      </div>
    </form>
  </div>
  <div class="col-sm-8">
    {{!-- @formatter:off --}}
<pre>\{{textarea-autosize
  class="form-control" ctrlEnterSubmitsForm?=false max-height="100px"
  placeholder="Ctrl+Enter will NOT submit nearest form."}}
</pre>
{{!-- @formatter:on --}}
  </div>
</div>

<hr/>
